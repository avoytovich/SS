import {makeStyles} from '@material-ui/core';

export const useStyles = makeStyles({
  contentWrapper: {
    display: 'flex',
    flexDirection: 'column',
    minHeight: '100vh',
    padding: '0',
    width: '100%',
    maxWidth: '1048px',
    margin: '0 auto'
  }
});
