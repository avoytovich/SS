import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  input: {
    width: '100%',
    height: 31,
  },
  cleanUpLink: {
    color: '#000',
    fontSize: 12,
    margin: 0,
    cursor: 'pointer',
  },
  formControl: {
    width: '100%',
  },
});
